---
import BaseLayout from './BaseLayout.astro';

interface Props {
  title: string;
  description: string;
  pubDate: Date;
  category: string;
  tags: string[];
}

const { title, description, pubDate, category, tags } = Astro.props;

const formattedDate = pubDate.toLocaleDateString('es-AR', {
  year: 'numeric',
  month: 'long',
  day: 'numeric',
});
---

<BaseLayout title={title} description={description}>
  <div class="container post-container">
    <header class="post-header mark-accent">
      <div class="post-meta">
        <span class="post-category">{category}</span>
        <time datetime={pubDate.toISOString()} class="post-date">{formattedDate}</time>
      </div>
      <h1 class="post-title">{title}</h1>
      <p class="post-description">{description}</p>
    </header>

    <div class="post-body">
      <slot />
    </div>

    <!-- BANNER SUSCRIPCIÓN -->
    <section class="subscribe-banner">
      <div class="subscribe-inner">
        <p class="subscribe-eyebrow">Recursos y artículos de PM e IA</p>
        <h2 class="subscribe-title">¿Te resultó útil este artículo?</h2>
        <p class="subscribe-desc">Subo recursos gratuitos y escribo sobre gestión de proyectos e inteligencia artificial. Sin spam.</p>
        <iframe width="540" height="305" src="https://a55096c1.sibforms.com/v2/serve/MUIFANq7Ejc7HcoQmak19v6LMCBF30dP4TIiNoM1t8h5eSbTtWQ4IfCj01uDg_8U55RsoMLNJS274dzOcW6sbYvfoBK6khQpZNNSUbra8acptq7niSl1gKxXuiLk8lOdtUH57JL4P-QCQjP2XiLqgZY5ki-bFDfXT9W4nYXUiJZgi30UOuLj8-5mdyiWfQA9v6o0zlm6hHMrx78WMg==" frameborder="0" scrolling="auto" allowfullscreen style="display:block;margin:0 auto;max-width:100%;border:none;"></iframe>
      </div>
    </section>

    <footer class="post-footer">
      <div class="post-tags">
        {tags.map(tag => (
          <span class="tag">{tag}</span>
        ))}
      </div>
      <a href="/blog" class="back-link">← Volver al blog</a>
    </footer>
  </div>
</BaseLayout>

<style>
  .post-container {
    padding-top: var(--space-lg);
    padding-bottom: var(--space-xl);
  }

  .post-header {
    margin-bottom: var(--space-lg);
  }

  .post-meta {
    display: flex;
    align-items: center;
    gap: 1rem;
    margin-bottom: var(--space-sm);
  }

  .post-category {
    font-size: 0.75rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.06em;
    color: var(--color-accent);
  }

  .post-date {
    font-size: 0.8rem;
    color: var(--color-muted);
  }

  .post-title {
    margin-bottom: 0.75rem;
  }

  .post-description {
    font-size: 1.1rem;
    color: var(--color-muted);
    margin-bottom: 0;
  }

  .post-body {
    margin-top: var(--space-md);
  }

  .post-body :global(h2) {
    margin-top: var(--space-md);
    margin-bottom: var(--space-sm);
  }

  .post-body :global(h3) {
    margin-top: var(--space-md);
    margin-bottom: var(--space-xs);
  }

  .post-body :global(ul),
  .post-body :global(ol) {
    padding-left: 1.5rem;
    margin-bottom: var(--space-sm);
  }

  .post-body :global(li) {
    margin-bottom: 0.35rem;
  }

  .post-body :global(blockquote) {
    border-left: 3px solid var(--color-accent);
    padding-left: var(--space-sm);
    color: var(--color-muted);
    font-style: italic;
    margin: var(--space-md) 0;
  }

  .post-body :global(code) {
    background: var(--color-surface);
    padding: 0.15em 0.4em;
    border-radius: 3px;
    font-size: 0.88em;
    font-family: 'Courier New', monospace;
  }

  .post-body :global(pre) {
    background: var(--color-surface);
    padding: var(--space-sm);
    border-radius: var(--radius);
    overflow-x: auto;
    margin-bottom: var(--space-sm);
  }

  .post-body :global(pre code) {
    background: none;
    padding: 0;
  }

  .post-body :global(hr) {
    border: none;
    border-top: 1px solid var(--color-border);
    margin: var(--space-md) 0;
  }

  /* BANNER SUSCRIPCIÓN */
  .subscribe-banner {
    margin-top: var(--space-xl);
    padding: var(--space-lg);
    background: var(--color-surface);
    border-radius: var(--radius);
    border-left: 3px solid var(--color-accent);
  }

  .subscribe-inner {
    max-width: 560px;
    margin: 0 auto;
    text-align: center;
  }

  .subscribe-eyebrow {
    font-size: 0.75rem;
    font-weight: 700;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--color-accent);
    margin-bottom: 0.5rem;
  }

  .subscribe-title {
    font-family: var(--font-display);
    font-size: 1.5rem;
    margin-bottom: 0.5rem;
  }

  .subscribe-desc {
    color: var(--color-muted);
    font-size: 0.95rem;
    margin-bottom: var(--space-md);
  }

  .post-footer {
    margin-top: var(--space-lg);
    padding-top: var(--space-md);
    border-top: 1px solid var(--color-border);
    display: flex;
    align-items: center;
    justify-content: space-between;
    flex-wrap: wrap;
    gap: 1rem;
  }

  .post-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
  }

  .tag {
    font-size: 0.75rem;
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    padding: 0.25rem 0.6rem;
    border-radius: 2rem;
    color: var(--color-muted);
  }

  .back-link {
    font-size: 0.85rem;
    font-weight: 600;
    color: var(--color-accent);
  }
</style>